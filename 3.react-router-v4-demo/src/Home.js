import React from "react";

const Home = () => {
  //pure 컴포넌트/dumb 컴포넌트 : state가 없거나 라이프사이클api 사용할 필요 없을 때 이렇게 선언 가능
  return (
    <div>
      <h2>home</h2>
    </div>
  );
};

export default Home;
